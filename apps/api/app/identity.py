"""Seeded local-owner identity constants (DEC-03).

Single source for every code path that materializes the pre-Phase-8
identity (seed command, first-run workspace creation), so the seeded
owner is identical regardless of which path runs first.
"""

LOCAL_OWNER_ID = "local-owner"
LOCAL_OWNER_DISPLAY_NAME = "Nick"
IDENTITY_SOURCE = "local_seed"
