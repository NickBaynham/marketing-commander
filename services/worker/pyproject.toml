[project]
name = "marketing-commander-worker"
version = "0.1.0"
description = "Marketing Commander worker (Phase 3 stub: Redis heartbeat only)"
requires-python = ">=3.14,<3.15"
dependencies = [
    "redis>=5",
]

[tool.pdm]
distribution = false
