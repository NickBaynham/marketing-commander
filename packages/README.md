# packages

Shared libraries used by more than one app or service (for example, typed
schema definitions shared by API and worker). Empty until a real shared
need exists — do not create speculative packages (CLAUDE.md: avoid
speculative infrastructure). The first candidate is the typed artifact
schemas in Phase 4 onward.
